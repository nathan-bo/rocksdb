name: test

on:
  workflow_dispatch:

jobs:

  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo hello world

  ci_workflow:
    uses: ./.github/workflows/ci.yml
#     secrets:
#       username: ${{ secrets.USERNAME }}
#       token: ${{ secrets.TOKEN }}
