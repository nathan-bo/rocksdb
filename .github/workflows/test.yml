name: test

on:
  workflow_dispatch:

jobs:
  ci_workflow:
    uses: nathan-bo/nathan-tests/.github/workflows/ci-workflow.yml@main
    with:
      username: ${{ secrets.username }}
      token: ${{ secrets.token }}
